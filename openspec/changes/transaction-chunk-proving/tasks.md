## 1. Precondition: chunk_trace field

- [x] 1.1 Add `pub chunk_trace: B256` field to `Precondition` struct in `crates/kona/src/precondition/mod.rs` with `#[rkyv(with = B256Def)]` annotation. Add `.chunk(chunk_trace: B256) -> Self` builder method. Ensure `Default` still produces all-zero fields.
- [x] 1.2 Extend `Digestible` implementation: when `chunk_trace` is non-zero, assert all other fields are zero, return `Digest::from_bytes(chunk_trace.0)`. Existing execution-only and derivation/proposal branches remain unchanged.
- [x] 1.3 Add unit tests for `Precondition::digest()`: chunk-only mode, execution-only mode (unchanged), derivation/proposal mode (unchanged), mixed-field assertion panics (chunk_trace + execution_trace, chunk_trace + derivation_cache, etc.), default is all-zero.
- [x] 1.4 Run existing `kailua-kona` test suite — verify zero regressions from the new field. Fix any rkyv serialization tests that depend on `Precondition` byte layout.

## 2. Memory DB canonical hashing and state normalization

- [x] 2.1 Create `crates/kona/src/precondition/chunking.rs` and add `pub mod chunking;` to `crates/kona/src/precondition/mod.rs`. This sits alongside the existing `execution.rs` (which has `exec_precondition_hash`, `attributes_hash`, `transactions_hash`) and follows the same deterministic SHA256 hashing pattern for precondition commitments. Define canonical state hashing functions that operate directly on revm's `DbAccount` instances and accept BOTH revm's `Cache` (from `CacheDB`, uses `DbAccount`/`AccountState`) and an effective State-backed flat view (from `State`, using `CacheAccount`/`AccountStatus` plus `State.block_hashes`, and optionally an initial preloaded `Cache` when execution runs on top of `CacheDB`), producing an identical byte encoding for the same logical state. For `Cache`-backed views, existing `DbAccount` entries are used as-is; for `CacheState`-backed views, `DbAccount` instances are constructed from `CacheAccount`/`PlainAccount` data. Reuse revm's existing `AccountState` enum as the canonical account state representation for hashing (no custom normalization enum or intermediate account type needed). Provide a public `normalize_account_status(status: &AccountStatus) -> AccountState` function that maps `AccountStatus` → `AccountState` equivalence for HASHING ONLY (Loaded→None, Changed→Touched, Destroyed→StorageCleared, InMemoryChange→Touched, etc.) and document the conversion details in the function.
- [x] 2.2 Implement `hash_cache()`, `hash_cache_state()`, and `hash_overlay_state()` functions in `precondition/chunking.rs` that compute deterministic SHA256 via streaming hasher over canonical encoding of `DbAccount` entries. Storage within each `DbAccount` (unordered `HashMap`) is sorted by slot key at encoding time. Canonical encoding: accounts (sorted by Address) with full account info + storage (sorted by U256) + account_state, then contract code_hashes only (sorted; raw bytecode not included since code_hash already commits to content), then block_hashes (sorted by U256 block number). All length prefixes use u64 BE. Explicitly EXCLUDE `Cache.logs` from this hash, since logs are committed by the EVM accumulator hash via receipts / logs bloom.
- [x] 2.3 Add unit tests: hash a `Cache` and a State-backed effective flat view that represent the same logical state → verify identical hashes. Empty state, single account, multiple accounts with storage, contracts present, block_hashes present. Add a case where execution started from a preloaded `Cache` and some entries remained untouched in the live `CacheState`, verifying the overlay recipe still hashes the full logical state. Verify any field change (nonce, balance, code_hash, storage slot, account_state, contract bytecode, block_hash) produces a different hash. Verify insertion order independence. Verify that log-only changes do NOT affect the state hash.
- [x] 2.2a Add `validate_cached_contracts()` in `precondition/chunking.rs` for future witness-cache ingestion. Raw bytecode may remain omitted from the hash encoding only if caches loaded from untrusted witness data are validated once against their keys (for example `bytecode.hash_slow() == code_hash`) before first hash / execution use. Do NOT perform this validation inside every memdb hash helper call.
- [x] 2.2b Make `hash_overlay_state()` lifecycle-aware for destroyed / storage-wiped accounts. When the projected live state normalizes to `AccountState::StorageCleared`, rebuild the effective hashed `DbAccount` from the live account info and live storage, dropping inherited base storage. When the projected live state represents a deleted / non-existent account, drop inherited base info and storage instead of fieldwise-merging them into the overlay.
- [x] 2.3a Add unit tests for contract-entry validation: a valid `(code_hash, bytecode)` pair can be validated successfully; modifying bytecode without updating the key causes `validate_cached_contracts()` to fail; replacing a contract with a different validated pair changes the hash.
- [x] 2.3b Add overlay-hashing tests for lifecycle edge cases: selfdestruct, destroy-and-recreate, and storage-cleared overlays must not retain inherited base storage or stale base account info in the effective hashed state.
- [x] 2.4 Create `hash_evm_state(evm_state: &EvmAccumulatorState) -> B256` in `precondition/chunking.rs`. Define `EvmAccumulatorState` struct with `cumulative_gas_used: u64`, `da_footprint_used: u64`, `blob_gas_used: u64`, `logs_bloom: Bloom`, `receipts: Vec<OpReceiptEnvelope>`. Hash with SHA256 over canonical encoding.
- [x] 2.5 Add unit tests for `hash_evm_state`: initial (zeroed) state, state with receipts, modification of any field changes the hash, receipt ordering matters.
- [x] 2.6 Create `compute_chunk_trace(tx_hash: B256, pre_db_hash: B256, post_db_hash: B256, pre_evm_hash: B256, post_evm_hash: B256) -> B256` in `precondition/chunking.rs`. Returns `SHA256(tx_hash || pre_db_hash || post_db_hash || pre_evm_hash || post_evm_hash)`.
- [x] 2.7 Add unit tests for `compute_chunk_trace`: determinism, any input change produces different output, integration with `Precondition::default().chunk(trace).digest()`.
- [x] 2.8 Run full `kailua-kona` test suite — verify zero regressions.

## 3. TracingOpEvm and TracingEvmFactory

- [x] 3.1 Create `crates/prover/src/evm.rs` (host-only). Define `TracingOpEvm<E: Evm>` struct wrapping any `E: Evm` with `traces: Arc<Mutex<Vec<EvmState>>>`. Follow the proven `CustomEvm` wrapper pattern from `op-reth/examples/custom-node/src/evm/alloy.rs`. Implement all 8 required `Evm` trait methods: `block()`, `chain_id()`, `finish()`, `set_inspector_enabled()`, `components()`, `components_mut()` delegate directly to inner. `transact_raw()` delegates then clones `result.state` into traces on success. `transact_system_call()` delegates transparently without appending to the tx-body trace buffer. No `Deref`/`DerefMut` needed — the block executor accesses fields through `Evm` trait default methods (`db()`, `db_mut()`, etc.) which delegate to `components()`. Note: `db()`, `db_mut()`, and 8 other provided defaults do NOT need manual implementation.
- [x] 3.2 Define associated types on `TracingOpEvm<E>` as pass-throughs: `type DB = E::DB`, `type Tx = E::Tx`, `type Error = E::Error`, `type HaltReason = E::HaltReason`, `type Spec = E::Spec`, `type BlockEnv = E::BlockEnv`, `type Precompiles = E::Precompiles`, `type Inspector = E::Inspector`. This makes the wrapper fully generic over any inner EVM.
- [x] 3.3 Define `TracingEvmFactory` struct in `crates/prover/src/evm.rs` (host-only) with `traces: Arc<Mutex<Vec<EvmState>>>`. Implement `EvmFactory` with `type Evm<DB, I> = TracingOpEvm<OpEvm<DB, I, PrecompilesMap>>` and matching associated types: `type Context<DB> = OpContext<DB>`, `type Tx = OpTransaction<TxEnv>`, `type Error<DBError> = EVMError<DBError, OpTransactionError>`, `type HaltReason = OpHaltReason`, `type Spec = OpSpecId`, `type BlockEnv = BlockEnv`, `type Precompiles = PrecompilesMap`. Both `create_evm` and `create_evm_with_inspector` delegate to `OpEvmFactory` then wrap the result. Add a `take_traces()` helper that atomically drains the shared trace buffer at the per-block boundary so stale traces do not leak across block executions. The factory must satisfy `EvmFactory<Spec = OpSpecId, BlockEnv = BlockEnv> + Send + Sync + Clone + 'static` to match `KonaExecutor`'s `Executor` impl bounds.
- [x] 3.4 Add unit tests for `TracingEvmFactory`: construct factory, create EVM, execute a simple transaction against an `InMemoryDB`, verify trace buffer contains exactly one entry with the expected touched addresses/slots. Test multiple transactions produce ordered tx-body trace entries. Test block-level `transact_system_call()` delegation does not append an extra trace entry. Test error propagation (failed tx → no trace entry). Test that trait-method field access (`.block()`, `.db_mut()`) works correctly on `TracingOpEvm`. Test that `take_traces()` drains the shared buffer so subsequent / cloned-factory executions start from an empty trace set.
- [x] 3.5 Run full `kailua-kona` test suite — verify zero regressions.

## 4. CachedExecutor EvmFactory generics

- [x] 4.1 Modify `CachedExecutor::new()` in `crates/kona/src/executor.rs`: change the specialized `impl` block from `CachedExecutor<KonaExecutor<'a, P, H, OpEvmFactory>>` to accept a generic `Evm: EvmFactory + Send + Sync + Clone + Debug` parameter, becoming `CachedExecutor<KonaExecutor<'a, P, H, Evm>>`. Pass the factory through to `KonaExecutor::new()`.
- [x] 4.2 Update all call sites of `CachedExecutor::new()` in `crates/kona/src/client/core.rs` to pass `OpEvmFactory::default()` explicitly (preserving current behavior).
- [x] 4.3 Run existing `test_cached_executor` and all `kailua-kona` tests — verify identical behavior. The refactor must be purely mechanical with no behavior change.

## 5. Chunk witness construction (host-side)

- [x] 5.1a Create `crates/kona/src/rkyv/chunking.rs` and add `pub mod chunking;` to `crates/kona/src/rkyv/mod.rs`. Define rkyv-serializable mirror types for revm's `Cache` structure: `SerializableCache` (with `SerializableDbAccount`, serializable `AccountInfo`, `Bytecode`, `AccountState`). Implement `From<Cache> for SerializableCache` and `From<SerializableCache> for Cache` conversions. Add round-trip tests. This sits alongside the existing rkyv modules (`execution.rs`, `optimism.rs`, `primitives.rs`, etc.) that already handle rkyv serialization for other revm/alloy types.
- [x] 5.1b `ChunkWitnessData` struct definition lives in `crates/kona/src/witness.rs` (co-located with `Witness`, shared with guest for deserialization). Host-only construction code (`build_chunk_witnesses`, `ChunkTxMeta`, `group_transactions_into_chunks`) lives in `crates/prover/src/chunk.rs`. Define `ChunkWitnessData` using `BlockEnv` and `OpBlockExecutionCtx` directly (serialized via `BlockEnvRkyv` and `OpBlockExecutionCtxRkyv` `ArchiveWith` wrappers in `rkyv/chunking.rs`), `Cache` (via `CacheRkyv`), and `EvmAccumulatorState` (direct rkyv derive with `OpReceiptRlpRkyv` for receipts via `rkyv::with::Map`). Fields: `block_number: u64`, `chunk_index: u16`, `total_chunks: u16`, `tx_start: u16`, `tx_count: u16`, `transactions: Vec<Vec<u8>>`, `block_env: BlockEnv`, `op_block_ctx: OpBlockExecutionCtx`, `cache: Cache`, `evm_state: EvmAccumulatorState`, `agreed_l2_output_root: B256`, `config_hash: B256`, `fpvm_image_id: B256`, `payout_recipient: Address`.
- [x] 5.2 Implement `group_transactions_into_chunks(tx_count: usize, max_txs_per_chunk: usize) -> Vec<Range<usize>>` in `crates/prover/src/chunk.rs` with the precondition `max_txs_per_chunk > 0`. Returns non-overlapping ranges covering all transactions.
- [x] 5.3 Add unit tests for `group_transactions_into_chunks`: even division, remainder, single chunk, single transaction per chunk, max >= total. Add a caller-side validation test that `max_txs_per_chunk = 0` is rejected before this helper is used.
- [x] 5.4 Implement `build_chunk_witnesses(...)` in `crates/prover/src/chunk.rs` (host-only). `ChunkTxMeta` SHALL carry any tx-body execution dependencies not recoverable from `EvmState` traces alone, including exact `BLOCKHASH` reads and per-transaction accumulator deltas such as `da_footprint_used` and `blob_gas_used`. Before chunking, pre-populate the cumulative cache with `NotExisting` entries for all addresses in any trace, plus all block hashes and pre-existing contract bytecodes from all traces/metadata. For each chunk: clone the full cumulative cache as the chunk's witness (not a filtered subset — required for hash chain continuity), record the EVM accumulator state at the chunk boundary, and advance the cumulative state through the chunk's traces for the next chunk.
- [x] 5.5 Add unit tests for `build_chunk_witnesses` in `crates/prover/src/chunk.rs`: single-chunk block (witness == post-prelude state), two-chunk block where chunk_1 reads chunk_0's storage write, same sender in chunk_0 and chunk_1 requiring nonce/balance carry-forward, contract creation in chunk_0 observed in chunk_1, selfdestruct in chunk_0 observed in chunk_1, contract bytecode inclusion, block_hash inclusion, EVM accumulator continuity between chunks.
- [x] 5.6 Run full `kailua-kona` test suite — verify zero regressions.

## 6. Chunk struct, PanicDB, and rkyv wrappers

- [x] 6.1a Define `Chunk` struct in `crates/kona/src/executor.rs` (co-located with `Execution`). Fields: `agreed_db: B256`, `agreed_evm: B256`, `tx_count: u16`, `tx_hash: B256`, `results: Vec<ResultAndState>` (full per-tx execution results), `evm_state: EvmAccumulatorState`, `claimed_db: B256`, `claimed_evm: B256`. `results.len() == tx_count`.
- [x] 6.1b Implement rkyv `ArchiveWith` wrappers for `ResultAndState` and its nested types in `crates/kona/src/rkyv/chunking.rs`: `ResultAndStateRkyv` (wrapping `ResultAndState<OpHaltReason>`), `ExecutionResultRkyv`, `EvmStateRkyv` (for `HashMap<Address, Account>`), `AccountRkyv` (for `Account` with `AccountInfo`, `AccountStatus` bitflags, `HashMap<U256, EvmStorageSlot>`). Add round-trip tests for each wrapper covering success/revert/halt variants, account lifecycle states, and storage slot values.
- [x] 6.2 Define `PanicDB` struct in `kailua-kona` implementing `DatabaseRef` trait (4 methods: `basic_ref`, `storage_ref`, `code_by_hash_ref`, `block_hash_ref`) that panics on all methods with descriptive error messages including the requested address/slot. Use `type Error = Infallible`. `CacheDB<PanicDB>` requires `PanicDB: DatabaseRef`. Note: `EmptyDB` returns defaults (not panics), so `PanicDB` is required for chunk proving.
- [x] 6.3 Add unit tests for `PanicDB`: each method panics with expected message.
- [x] 6.4 Add rkyv round-trip tests for `Chunk`: verify serialization/deserialization preserves all fields including the per-tx `ResultAndState` entries.
- [x] 6.5 Run full `kailua-kona` test suite — verify zero regressions.

## 7. Chunk execution-only mode in run_core_client

- [x] 7.0 Define `compute_tx_hash(transactions: &[Vec<u8>]) -> B256` in `precondition/chunking.rs`. Use deterministic SHA256 with explicit encoding: `SHA256(u64_be(len) || for each tx: u64_be(tx.len()) || tx_bytes)`, matching the length-prefix pattern used by the existing memdb hashing functions. Add unit tests: determinism, ordering sensitivity, content sensitivity, empty list.
- [x] 7.1 Add `chunk_witness: Option<ChunkWitnessData>` field to `Witness` in `crates/kona/src/witness.rs`. Add rkyv serialization with `Default` producing `None`. Thread it through `run_stateless_client()` → `run_stitching_client()` → `run_core_client()` as a new parameter.
- [x] 7.2 Add the chunk execution-only branch in `run_core_client()` (`crates/kona/src/client/core.rs`): when `boot.l1_head == B256::from([0xFF; 32])`, enter chunk mode. Extract `ChunkWitnessData` from the parameter (panic if `None`).
- [x] 7.3 Implement the chunk execution flow within the new branch:
  (a) Validate every cached contract entry against its `code_hash` key via `validate_cached_contracts()`.
  (b) Build `CacheDB<PanicDB>` from `witness.cache`.
  (c) Compute `pre_db_hash` and `pre_evm_hash` from the witness state.
  (d) Wrap in `State::builder().with_database(cache_db).build()`, set state-clear flag from block/hardfork predicate. Do NOT enable bundle updates — the post-state is computed from the live `CacheState`/`block_hashes`, not from the bundle delta.
  (e) Drive transaction execution through upstream's `OpBlockExecutor` (from `alloy-op-evm`) with its `gas_used`, `da_footprint_used`, and `receipts` accumulators seeded from `witness.evm_state`. Invoke the executor's `execute_transaction_without_commit` + `commit_transaction` per transaction — but do NOT call `apply_pre_execution_changes` (chunk_0 witness already reflects the prelude) or `finish` (no post-block balance increments for chunks). Accumulate `logs_bloom` locally from each committed receipt, since `OpBlockExecutor` does not track a running bloom.
  (f) Compute `tx_hash` from `witness.transactions`.
  (g) Compute `post_db_hash` from the effective post-state view (overlay final `CacheState` onto initial witness `Cache`), compute `post_evm_hash`.
  (h) Compute `chunk_trace = compute_chunk_trace(tx_hash, pre_db_hash, post_db_hash, pre_evm_hash, post_evm_hash)`.
  (i) Return `(boot, Precondition::default().chunk(chunk_trace))`.
- [x] 7.4 Add integration test: construct a `ChunkWitnessData` for known simple transactions (transfers, storage writes), run chunk mode, verify the returned `ProofJournal` has `l1_head == 0xFF..FF` and `precondition_hash` matches manual computation of `chunk_trace`.
- [x] 7.5 Add integration test: multi-chunk block. Build witnesses for 2 chunks. Execute both in chunk mode. Verify `post_db_hash` of chunk_0 matches `pre_db_hash` of chunk_1's witness.
- [x] 7.6 Add test: missing state in witness triggers PanicDB panic. Add test: malformed contract bytecode is rejected before hashing/execution.
- [x] 7.7 Run full `kailua-kona` test suite — verify zero regressions.
- [x] 7.8 Refactor chunk execution flow to reuse upstream `OpBlockExecutor` with seeded accumulators (`gas_used`, `da_footprint_used`, `receipts` fields, all `pub` in upstream) instead of a hand-rolled per-transaction loop. Skip `apply_pre_execution_changes` and `finish` to preserve the post-prelude-to-pre-epilogue scope. Eliminates ~160 lines of manually mirrored per-tx logic (block-available-gas precheck, Jovian DA footprint estimation + L1Block preload, manual `OpReceiptEnvelope::from_parts` construction, manual `depositor.unwrap_or_default()` deposit-nonce read). No behavioral change — byte-exact `chunk_trace` match with pre-refactor outputs is verified.
- [x] 7.9 Harden the witness trust boundary for `OpBlockExecutor` seeding. Upstream's per-tx prechecks use unchecked `gas_limit - self.gas_used` and `gas_limit - self.da_footprint_used` subtraction, so a malformed witness that seeds either counter above `cw.block_env.gas_limit` wraps to near-`u64::MAX` in release (silently accepting an impossible "huge" budget) or panics in debug. Reject such witnesses at chunk entry with explicit bounds checks. Also enforce the OP Jovian invariant `blob_gas_used == da_footprint_used` at chunk entry — upstream's `BlockExecutionResult.blob_gas_used = self.da_footprint_used` makes them the same quantity at block boundaries, and OP L2 user transactions cannot consume real EIP-4844 blob gas (no `Eip4844` variant in `OpTxType`). With the invariant enforced, derive the output `blob_gas_used` from `exec.da_footprint_used`, matching upstream's convention by construction and preserving chunk-to-chunk continuity when the host threads the two fields in lockstep. Add rejection regressions: `test_chunk_mode_rejects_blob_gas_ne_da_footprint`, `test_chunk_mode_rejects_cumulative_gas_above_limit`, `test_chunk_mode_rejects_da_footprint_above_limit`. Add positive regression `test_chunk_mode_accepts_synced_nonzero_seeds` exercising a mid-block chunk with non-zero synced seeds (1M gas, 500 DA, 500 blob).

## 8. ChunkingEvm, ChunkingEvmFactory, and chunk aggregation

- [x] 8.1 Define `ChunkingEvm<E: Evm>` in `kailua-kona` (analogous to `TracingOpEvm`). Wraps inner `E: Evm`, holds a chunk vec (reversed, popped from end) and current tx index. `transact_raw()`: if a chunk is active, return the next pre-computed `ResultAndState` from `chunk.results`; when the active chunk is exhausted, pop the next pending chunk (chunks cover consecutive txs starting at index 0 per Decision 12); if all chunks are exhausted, delegate to inner EVM. `transact_system_call()`: always delegates to inner (prelude/epilogue run normally). All other `Evm` trait methods delegate to inner. (Note: `tx_start` tracking from the original task brief is implicit — chunks are consumed in order; the `Chunk` struct itself does not carry a `tx_start` field, see `crates/kona/src/executor.rs`.)
- [x] 8.2 Define `ChunkingEvmFactory` (analogous to `TracingEvmFactory`). Implements `EvmFactory` with `type Evm<DB, I> = ChunkingEvm<OpEvm<DB, I, PrecompilesMap>>`. Holds per-block chunk data (`Arc<Mutex<HashMap<u64, Vec<Chunk>>>>`, keyed by block number). Both `create_evm` and `create_evm_with_inspector` read `input.block_env.number` to destructively remove that block's chunks from the map, then delegate to `OpEvmFactory` and wrap the result in `ChunkingEvm`.
- [x] 8.3a Add `chunks: Vec<Vec<Chunk>>` field to `Witness` (one inner vec per block, empty by default). `Chunk` already derives rkyv via `ResultAndStateRkyv`, so no additional wrappers are needed.
- [x] 8.3b Extend the `StitchingClient` trait signature and `KonaStitchingClient` implementation to accept and forward chunk data. Extend `run_core_client()` parameter list to accept chunk data. Update all call sites in `run_stateless_client()`, `run_stitching_client()`, `run_core_client()`, `kailua-hana`, `kailua-hokulea`, and `kailua-prover::client::witgen`.
- [x] 8.3c In `run_core_client()`, always construct `ChunkingEvmFactory` (keyed by block number: outer index `i` maps to block `safe_head_number + 1 + i`) for `CachedExecutor::new()`. An empty chunks map is semantically identical to `OpEvmFactory` (every `transact_raw()` delegates), so this unifies the code path without branching on chunk presence.
- [x] 8.4 Implement `stitch_chunks()` in `crates/kona/src/client/stitching.rs` (analogous to `stitch_executions()`). Called in `run_stitching_client()` after `run_core_client()` returns. For each block's chunks: reject malformed witnesses (`chunk.tx_count != chunk.results.len()`), verify hash chain continuity (`chunk[i].agreed_db == chunk[i-1].claimed_db`, same for `agreed_evm`/`claimed_evm`), compute `chunk_trace` via `compute_chunk_trace`, construct expected `ProofJournal` using each chunk's own `agreed_l2_output_root` and `parent_block_number` (per-block context stored in `Chunk` itself — not derived from the outer run's final `BootInfo`, which would collapse multi-block journals to the run's last block), call `verify_stitching_journal()`.
- [x] 8.5 Add unit tests for `ChunkingEvm`: pre-computed results returned in order (marker gas_used), system calls delegate to inner and do not advance tx index, empty chunk vec delegates all transact_raw to inner, multi-chunk cursor crosses boundary, factory routes by block number, `take_chunks` is destructive per block, exhausted chunks delegate to inner.
- [x] 8.6 Add unit tests for `stitch_chunks()`: empty chunks no-op, single chunk passes, valid multi-chunk hash chain passes, `agreed_db` mismatch panics, `agreed_evm` mismatch panics, empty blocks between chunked blocks are skipped, `tx_count != results.len()` malformed-witness is rejected, multi-block chunks use per-chunk `agreed_l2_output_root` / `parent_block_number` instead of outer `BootInfo`.
- [x] 8.7 Host-capture → guest-replay round-trip integration test. Runs the client twice against a real OP Sepolia fixture: first pass captures per-tx `ResultAndState` through a trace-collecting `ChunkingEvmFactory`, then rebuilds `Chunk` entries from the captured data and replays on a second pass through the CHUNK VERIFY cross-checks.
  - **Production change: per-block trace capture in `ChunkingEvmFactory`/`ChunkingEvm`.**
    Added `ChunkTraceCollector = Arc<Mutex<HashMap<u64, Vec<ResultAndState<OpHaltReason>>>>>` type alias and `new_with_traces(chunks, traces)` constructor. When `Some`, every `ChunkingEvm::transact_raw` success appends the `ResultAndState` into `block_traces[block_number]` — same capture semantic as `TracingEvm`, folded directly into the factory so one factory type can serve both passes.
  - **Production fix: account pre-load on chunk replay.** `ChunkingEvm::transact_raw`'s replay branch now calls `Database::basic(addr)` + `Database::storage(addr, slot)` on every account/slot in the replayed state diff before returning. Without this, the executor's downstream `State<TrieDB>::commit` would panic in revm (`apply_account_state` requires the address be cache-resident) because the replayed path skips the DB reads that would normally load accounts. Restores the invariant that the commit step behaves identically whether the `ResultAndState` came from live execution or chunk replay.
  - **Production change: `run_core_client` accepts optional `chunk_trace_collector: Option<ChunkTraceCollector>`.** Threaded through to `ChunkingEvmFactory::new_with_traces`. All 5 callers updated (hana, hokulea, prover/witgen, core tests, stitching).
  - **New tests in `crates/kona/src/client/core.rs`:**
    - `test_op_sepolia_16491249_16491250_chunks_roundtrip` — 1-block fixture, single chunk per block.
    - `test_op_sepolia_16491249_16491349_chunks_roundtrip` — 100-block fixture, single chunk per block (100 independent block_env / op_block_ctx cross-checks, 100 tx_hash bindings, 100 receipts_root reconstructions).
    - `test_op_sepolia_16491249_16491250_two_chunks_roundtrip` — 1-block fixture, 2 chunks per block. Exercises `ChunkingEvm`'s cursor advance across chunks, hash-chain continuity (`chunks[i].agreed_evm == chunks[i-1].claimed_evm`), per-chunk `tx_hash` binding over tx slices, incremental `evm_state` construction where only the LAST chunk matches the block totals.
    - `test_op_sepolia_16491249_16491250_with_empty_chunks` / `test_op_sepolia_16491249_16491349_with_empty_chunks` — empty-chunks pass-through sanity tests from the earlier pass.
  - **New test helpers:**
    - `test_derivation_with_chunks_and_traces(...)` — variant of `test_derivation_with_chunks` that also threads the trace collector.
    - `build_single_chunk_for_block(execution, parent_block_number, traces) -> Chunk` — assembles a full-block chunk from derivation output + captured traces.
    - `build_n_chunks_for_block(execution, parent_block_number, traces, n) -> Vec<Chunk>` — assembles `n` sequential chunks covering the block, with incremental cumulative `evm_state` per chunk and hash-chain continuity between them.
  - **Status:** 198/198 `kailua-kona` tests pass (baseline 195 + 3 new round-trips). Workspace clippy clean.
- [x] 8.8 Run full `kailua-kona` test suite — verify zero regressions. 177/177 tests pass (baseline 154 + 8 ChunkingEvm + 9 stitch_chunks + 7 hash_results + 1 chunk_rkyv field-growth test).
- [x] 8.9 Close the `Chunk.results` ↔ `chunk_trace` binding gap via option (a) — extend `chunk_trace` to commit to `results_hash = SHA256(canonical_encode(results))`. Implementation:
  - Added `hash_results()` in `crates/kona/src/precondition/chunking.rs` — canonical SHA256 over `Vec<ResultAndState<OpHaltReason>>` with deterministic byte encoding (sorted per-address, per-slot; excludes transient `transaction_id` / `is_cold` / `original_info` / `AccountInfo.code` fields). 7 unit tests cover empty, order-sensitivity, gas/variant/state sensitivity, and transient-field invariance.
  - Extended `compute_chunk_trace` from 5 to 6 inputs: `SHA256(tx_hash || pre_db_hash || post_db_hash || pre_evm_hash || post_evm_hash || results_hash)`.
  - Moved `TracingEvm` / `TracingOpEvmFactory` from `crates/prover/src/evm.rs` to `crates/kona/src/evm.rs` (re-exported from prover) so both the host (block-level capture for chunk witness construction) and the chunk guest (chunk-level capture feeding `results_hash`) share one implementation.
  - Chunk guest (`run_core_client` chunk branch) now wraps the inner `OpEvm` with `TracingOpEvmFactory`, captures per-tx `ResultAndState`, sanity-checks `traces.len() == transactions.len()`, and folds `hash_results(&traces)` into `chunk_trace`.
  - `stitch_chunks` recomputes `hash_results(&chunk.results)` on the aggregation side and uses the same six-input formula to rebuild the expected chunk journal — any tampering with `chunk.results` (reorder, substitute, forge) changes the reconstructed `chunk_trace` so `env::verify()` rejects the tampered chunk proof assumption.
  - Spec updates: chunk-proving `spec.md` — new requirement *Chunk guest commits to per-tx results_hash via chunk_trace* with 4 scenarios. `design.md` Decision 5 — updated to describe six-input `chunk_trace` plus the canonical results encoding. Decision 12 addendum — added a fourth chunk-side difference capturing the tracing wrapper.
  - Test: `stitch_chunks_results_tampering_changes_chunk_trace` asserts that altering a single `gas_used` field in `Chunk.results` produces a different `chunk_trace` — the linchpin invariant that makes `env::verify` reject tampered witnesses in the zkvm build.
- [x] 8.12 Close Codex round-4 adversarial review findings:
  - **[critical] Blob-pricing context now authenticated in `verify_block_chunks`.** Previously the check explicitly skipped `chunk.block_env.blob_excess_gas_and_price`. Since the aggregation path replays witness-supplied `chunk.results` (no independent re-execution), pre/post EVM hash continuity was only internal consistency between attacker-controlled chunks — a prover could have authenticated a chunk proof under a forged blob-pricing context (wrong BLOBBASEFEE, wrong blob-gas accounting) and had aggregation accept the wrong post-state. Fix:
    - Added `expected_blob_excess_gas_and_price(parent_header, spec_id)` in `crates/kona/src/precondition/chunking.rs` — mirrors kona's `prepare_block_env` (same BlobParams + update fraction selection by spec_id).
    - Extended `verify_block_chunks` to accept `expected_blob_excess_gas_and_price: Option<BlobExcessGasAndPrice>` and require strict equality against `chunk.block_env.blob_excess_gas_and_price`.
    - `run_core_client` CHUNK VERIFY phase now computes the expected value from the derivation pipeline's authenticated parent header (safe_head for block[0], collected[i-1] for later blocks) + current-block timestamp's `spec_id`, and passes it in.
    - Regression test `verify_block_chunks_rejects_blob_price_forgery` asserts rejection.
  - **[high] Chunks plumbed through `run_witgen_client` → `Witness`.** Previously the main witgen entrypoint always called `run_core_client` with `chunks = Vec::new()` and emitted `Witness { chunks: Vec::new(), .. }`, so the transaction-chunk-proving path was unreachable end-to-end. Fix:
    - `run_witgen_client` now takes a `chunks: Vec<Vec<Chunk>>` parameter.
    - Chunks are cloned once up front, threaded into `run_core_client`, and serialized into `Witness.chunks`. When chunks are non-empty, the helper drops its own `execution_trace` (since `run_core_client` requires it to be `None` under chunks — repurposed slot).
    - Callers (hana/hokulea/proving.rs) updated to pass `Vec::new()` for now; Part 9's prover dispatch will provide the non-empty vec. This closes the plumbing gap so downstream work can trivially wire chunks in.
  - **End-to-end stateless replay test** (`test_stateless_client_with_chunks` in `crates/kona/src/client/stateless.rs`). Follows the existing `test_stateless_client` pattern exactly — captures real derivation data with `test_derivation_with_chunks_and_traces` (using the cached `testdata/` `TestOracle` fixture), then sets `l1_head = ZERO` and feeds `Witness { stitched_executions, chunks, .. }` through `run_stateless_client`. No blob witness required because derivation isn't re-run in pass 2.
  - **EXECUTION-ONLY branch extended with chunks support**. Previously the EXECUTION-ONLY branch (`boot.l1_head.is_zero()`) in `run_core_client` used `OpEvmFactory` directly and had no CHUNK VERIFY phase. Now it constructs a `ChunkingEvmFactory` (empty map if no chunks → behaves identically to `OpEvmFactory` per-tx-delegation), and runs a CHUNK VERIFY phase against the cached Executions — same cross-checks as the DERIVATION branch (output-root anchor, block_env + blob pricing, tx_hash, hash_evm_state, receipts_root, hash-chain continuity). This unlocks the existing `test_stateless_client` pattern for chunks: pass 1 captures Executions + traces via real derivation, pass 2 sets `l1_head = ZERO` so derivation doesn't run (no blobs needed) yet the full chunk cross-check machinery still authenticates every chunk.
  - Status: 200/200 `kailua-kona` tests pass. Workspace clippy clean.

- [x] 8.11 Close the third-round adversarial review critical finding by binding the chunk's block execution context into `chunk_trace` and adding an aggregation-side header cross-check in `verify_block_chunks`. Implementation:
  - Extended `compute_chunk_trace` from 6 to 7 inputs — added `block_ctx_hash`: `SHA256(tx_hash || pre_db_hash || post_db_hash || pre_evm_hash || post_evm_hash || results_hash || block_ctx_hash)`.
  - Added `hash_block_ctx(block_env, op_block_ctx)` canonical encoder in `precondition/chunking.rs`. Encodes every BlockEnv field (number, beneficiary, timestamp, gas_limit, basefee, difficulty, prevrandao, blob_excess_gas_and_price) and every OpBlockExecutionCtx field (parent_hash, parent_beacon_block_root, extra_data).
  - Added `block_env: BlockEnv` and `op_block_ctx: OpBlockExecutionCtx` fields to `Chunk` (rkyv via existing `BlockEnvRkyv` / `OpBlockExecutionCtxRkyv`).
  - Chunk guest (`run_core_client` chunk branch) computes `block_ctx_hash = hash_block_ctx(&cw.block_env, &cw.op_block_ctx)` and folds into `chunk_trace`.
  - `stitch_chunks` reconstructs `block_ctx_hash` from each chunk's carried `block_env`/`op_block_ctx` and uses it in the six→seven-input `compute_chunk_trace` for journal reconstruction.
  - `verify_block_chunks` now takes `block_header: &Header` and cross-checks every `chunk.block_env` / `chunk.op_block_ctx` field against the derivation pipeline's actual sealed header (`number == U256::from(header.number)`, `timestamp == U256::from(header.timestamp)`, `beneficiary == header.beneficiary`, `gas_limit == header.gas_limit`, `basefee == header.base_fee_per_gas.unwrap_or(0)`, `difficulty == header.difficulty`, `prevrandao == Some(header.mix_hash)`, `parent_hash == header.parent_hash`, `parent_beacon_block_root == header.parent_beacon_block_root`, `extra_data == header.extra_data`). `blob_excess_gas_and_price` is bound indirectly via `pre/post_evm_hash` continuity plus the `block_ctx_hash` commitment.
  - 5 new unit tests: `verify_block_chunks_rejects_block_env_timestamp_mismatch`, `_basefee_mismatch`, `_coinbase_mismatch`, `_op_block_ctx_parent_hash_mismatch`, `_op_block_ctx_extra_data_mismatch`. Covers BASEFEE / PREVRANDAO / COINBASE / TIMESTAMP / BLOCKHASH / Holocene-Jovian extra_data forgery vectors.
  - Spec updates: `specs/chunk-precondition/spec.md` (seven-input `chunk_trace`), `specs/chunk-proving/spec.md` (new scenarios for `block_ctx_hash` inclusion and sensitivity), `specs/chunk-aggregation/spec.md` (new requirement *Aggregation-side block-coherence verification via `verify_block_chunks`* with 5 scenarios), `design.md` Decision 5 (rationale + canonical encoding documentation).
- [x] 8.10 Close the second-round adversarial review findings (H-1, H-2, M-1) by adding an aggregation-side chunk→block coherence check. Introduces `verify_block_chunks` in `crates/kona/src/precondition/chunking.rs` and a `CHUNK VERIFY` phase in `run_core_client` that enforces, for every block with chunk data:
  - **Output-root anchor (item 1, H-1/H-2 equivalent via L2 output root)** — `chunk.agreed_l2_output_root == derived_parent_output_root` and `chunk.parent_block_number == safe_head_number + block_position`. Uses the L2 output root as the cryptographic anchor because it commits to the state root and therefore to all L2 state. Chosen over a literal `hash_cache_state(State<TrieDB>)` match because the chunk witness `Cache` pre-populates `NotExisting` stubs for all block-accessed addresses, while aggregation's `State<TrieDB>.cache` at the same boundary only holds prelude-touched entries — the raw hashes would never match without either a major witness restructure or transporting the full witness cache to aggregation. The L2-output-root anchor is at least as strong (collision-resistant state_root commitment) without either cost.
  - **`tx_hash` binding (item 2)** — `compute_tx_hash(block_txs[cursor..cursor+tx_count]) == chunk.tx_hash` for each chunk, where `block_txs` comes from the derivation pipeline's `OpPayloadAttributes.transactions` captured via `CachedExecutor.collection_target`. Forces the chunk proof to authenticate exactly the derivation-output txs (closes T_Y ≠ T_X substitution).
  - **Full coverage** — `sum(chunk.tx_count) == block_txs.len()`. Rejects partial coverage that would let the tail run live (review finding M-9).
  - **evm_state self-consistency (item 3 part 1)** — `hash_evm_state(&chunk.evm_state) == chunk.claimed_evm` for every chunk. Makes the structural `evm_state` field safely authenticated via the hash-chain.
  - **Receipts-root reconstruction (item 3 part 2)** — `calculate_receipt_root(&last_chunk.evm_state.receipts) == block.header.receipts_root`. Independent check that the authenticated cumulative receipts from the chunk guest reproduce the block executor's receipts_root; catches any Frankenstein receipts produced by replaying a different-tx result pair.
  - **Hash-chain continuity** — `chunks[i+1].agreed_db == chunks[i].claimed_db` and likewise for `agreed_evm`/`claimed_evm`. Redundant with `stitch_chunks`, kept here as defense-in-depth.
  - 10 unit tests in `verify_block_chunks_*` covering each rejection mode plus the happy path.
  - Inline documentation added for all remaining review findings (M-2, M-3, M-4, M-5, M-6, M-8, M-9, M-10, L-1, L-2) in `precondition/chunking.rs`, `evm.rs`, and `executor.rs`. Spec doc drift (L-3) fixed in `specs/chunk-precondition/spec.md`.

## 9. Prover-side chunk dispatch

- [ ] 9.1 Add `max_txs_per_chunk: usize` field to `ProvingArgs` in `crates/prover/src/args.rs` with default `usize::MAX`, `#[clap(long)]`, and validation that rejects `0`.
- [ ] 9.2 In `crates/prover/src/tasks.rs`, add chunk dispatch logic: only when `0 < max_txs_per_chunk < block_tx_count`, pre-execute with `TracingEvmFactory` (from `crate::evm`, which now captures full `ResultAndState` per tx), call `build_chunk_witnesses()` (from `crate::chunk`), construct `Chunk` entries with per-tx `ResultAndState` instances and computed hash commitments (`agreed_db`/`claimed_db`/`agreed_evm`/`claimed_evm`/`tx_hash`), dispatch chunk proof jobs in parallel via `seek_proof()`, collect receipts. Otherwise keep the existing monolithic proving path for that block.
- [ ] 9.3 After chunk receipts are collected, assemble the chunk data for the aggregation proof: `Vec<Chunk>` entries (with per-tx `ResultAndState`, hash commitments, and `EvmAccumulatorState`) plus chunk proof receipts appended to `stitched_proofs`.
- [ ] 9.4 Add integration test: mock a block with known transactions, set `max_txs_per_chunk`, verify chunk witnesses are constructed correctly and chunk proofs are dispatched.
- [ ] 9.5 Add integration test: verify that `max_txs_per_chunk = usize::MAX` produces identical behavior to the current code (no chunking path activated).

## 10. End-to-end validation

- [ ] 10.1 Create an end-to-end test that proves a known block both monolithically and via 2-chunk decomposition. Verify both produce identical `ProofJournal` (same precondition_hash at the block level, same agreed/claimed output roots, same block number).
- [ ] 10.2 Create an end-to-end test with 3+ chunks where cross-chunk state dependencies exist (chunk_1 reads chunk_0's write, chunk_2 depends on prior chunk account metadata such as nonce, balance, or contract creation). Verify the aggregation proof produces correct state_root.
- [ ] 10.3 Create an end-to-end test combining block-level stitching (`max_block_executions`) with transaction chunking (`max_txs_per_chunk`). Verify the composed proof is valid.
- [ ] 10.4 Run the full Kailua test suite (all crates) and verify zero regressions across the entire codebase.
